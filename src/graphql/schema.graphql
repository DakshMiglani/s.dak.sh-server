type Query {
	getDetails(uuid: ID!): DetailsResponse!
}

type Mutation {
	create(redirectTo: String!, createdBy: String): String!
}

type DetailsResponse {
	uuid: ID!
	redirectTo: String!
	views: Int!
	createdBy: String
	createdOn: String!
	updatedOn: String
}
