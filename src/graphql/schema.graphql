type Query {
	getDetails(uuid: ID!): URL!
}

type Mutation {
	create(redirectTo: String!, createdBy: String): URL!
}

type URL {
	uuid: ID!
	redirectTo: String!
	views: Int!
	createdBy: String
	createdOn: String!
	updatedOn: String
}
